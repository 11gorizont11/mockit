version: '3.7'

services:
  db:
    image: mongo
    networks: 
      - backend
    volumes:
      - mongo-data:/data/db
    hostname: mongo
    restart: always
    ports:
      - "27017:27017"
  api:
    build:
      context: ./api
      dockerfile: Dockerfile.dev
    networks:
      - backend
    ports:
      - '4000:4000'
    depends_on:
      - db
    links:
      - db
    environment:
      - MONGO_URL=mongodb://db
  web:
    build:
      context: ./web
      dockerfile: Dockerfile
    ports:
      - '8080:8080'
    depends_on:
      - api
      - db
    links:
      - api
    environment:
      - API_URL=api
volumes:
  mongo-data:
networks:
  backend:
    driver: overlay
version: '3.7'

services:
  db:
    image: mongo
    restart: always
    ports:
      - '27017:27017'
  api:
    build:
      context: ./api
      dockerfile: Dockerfile.dev
    volumes:
      - './api/src:/app/src'
    ports:
      - '4000:4000'
    depends_on:
      - db
